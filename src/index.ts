import Cleanmate from './cleanmateService';
import CleanmateMap from './cleanmateMap';
import {
  WorkMode,
  WorkState,
  MopMode,
} from './types';
import type { Status, MapData, Point, Region } from './types';

export default Cleanmate;
export {
  CleanmateMap,
  WorkMode,
  WorkState,
  MopMode,
};
export type {
  Status,
  MapData,
  Point,
  Region,
};